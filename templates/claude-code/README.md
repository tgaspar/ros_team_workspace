# About this directory

This directory contains the configuration for Claude Code that we use in our daily development.

Ideally, they would be placed in a RTW-managed workspace.
But they can also be used as the main Claude configuration and therefore be placed in `~/.claude`


